$(function(){  					// sidebar toggle    var sidebarMenu = function() {        if( !$("body").hasClass('swipe') ){            $("body").addClass("swipe");            if ( !$("#mask").length > 0 ) {                $("#sidebarMenu").before("<div id='mask'></div>");            }        }else{            $("body").removeClass("swipe");        }    };        // sidebar toggle    $(".navbar-toggle").on("click", function() {    	console.log("navbar-toggle");        sidebarMenu();    });        $("body").on("click", "#mask", function (e) {        $.sidebarClose();    });        $.extend({       sidebarClose : function(){           $("body").removeClass("swipe");           $("#sidebarMenu").removeClass("in");       }     }); 	// GNB	if (Modernizr.touch){ 		/* ---------------------------------------------------------------------- */		/*  Mobile		/* ---------------------------------------------------------------------- */			} else {		/* ---------------------------------------------------------------------- */		/*  PC		/* ---------------------------------------------------------------------- */		$("#gnb > li").on("mouseenter focusin", function(){	    	$(this).addClass("on").children("ul").stop().slideDown();	    }).on("mouseleave focusout", function(){	    	$(this).removeClass("on").children("ul").stop().slideUp();	    });	}        	//affix    $('#header').affix({		offset: {			top: 40		}	});    	// Top 스크롤     $('#anchorTop').affix({        offset: {top: 50}    });        $("#anchorTop").click(function(e){        e.preventDefault();        $("html,body").animate({scrollTop:0},300);    });});$(document).ready(function() {	// slider    $('.slider').slick({		dots: true,		infinite: true,		speed: 300,		slidesToShow: 1,		arrows: false,		autoplay: false 	});});